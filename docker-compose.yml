version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: app/api/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    environment:
      MODEL_PATH: models/saved/cnn_best.pth
    restart: unless-stopped
